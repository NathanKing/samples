<%= form_for [@course, @announcement] do |f|%>
	<div class="field">
		<%= f.label :title%><br />
		<%= f.text_field :title%>
	</div>
	<div class="field">
		<%= f.label :text%><br />
		<%= f.text_area :text, :rows => 3 %>
	</div>
	<div class="actions">
		<%= f.submit %>
	</div>

<%end%>
